
<%= form_for @post do |p| %>
  <%= p.label      :title %>
  <%= p.text_field :title %>
  <br><br>


  <%= p.collection_check_boxes :category_ids, Category.all, :id, :name %>
  <br>
  <%= p.label "New category:" %>
  <%= p.fields_for :categories, post.categories.build do |categories_fields| %>
    <%= categories_fields.text_field :name %>
  <% end %>
  <br><br>

  <%= p.label      :content %>
  <%= p.text_area :content %>

  <%= p.submit "Create Post" %>

<% end %>
